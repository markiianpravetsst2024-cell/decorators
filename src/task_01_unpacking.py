def split_first_last(values: list[int]) -> tuple[int, list[int], int]:
    """
    Unpack `values` into its first element, middle elements, and last element.

    Use Python unpacking syntax inside the function body.

    Args:
        values: A list of at least 2 integers.

    Returns:
        A tuple of (first, middle, last) where `middle` is a list.

    Example:
        >>> split_first_last([10, 20, 30, 40])
        (10, [20, 30], 40)

        >>> split_first_last([1, 2])
        (1, [], 2)
    """
    raise NotImplementedError
